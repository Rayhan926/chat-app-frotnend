import LoginScreen from '@views/LoginScreen';
import Head from 'next/head';

const Login = () => (
  <>
    <Head>
      <title>Login</title>
    </Head>
    <LoginScreen />
  </>
);

export default Login;
